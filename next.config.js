/** @type {import("next").NextConfig} */
const config = {
  images: {
    domains: ["127.0.0.1", "loremflickr.com", "cloudflare-ipfs.com", "placehold.co", "avatars.githubusercontent.com"],
  },
};

export default config;
